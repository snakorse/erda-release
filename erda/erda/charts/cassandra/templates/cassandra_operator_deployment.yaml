apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.cassandra.operatorName | default "cassandra-operator" }}
  namespace: {{ .Values.global.namespace | default "erda-system" }}
  labels:
    app:  {{ .Values.cassandra.operatorName | default "cassandra-operator" }}
    operator: {{ .Values.cassandra.operatorName | default "cassandra-operator" }}
spec:
  replicas: 1
  selector:
    matchLabels:
      name:  {{ .Values.cassandra.operatorName | default "cassandra-operator" }}
  strategy:
    rollingUpdate:
      maxUnavailable: 100%
      maxSurge: 0%
  template:
    metadata:
      labels:
        name:  {{ .Values.cassandra.operatorName | default "cassandra-operator" }}
        app:  {{ .Values.cassandra.operatorName | default "cassandra-operator" }}
        operator: {{ .Values.cassandra.operatorName | default "cassandra-operator" }}
    spec:
      {{- if .Values.global.imagePullSecrets }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- end }}
      serviceAccountName:  {{ .Values.cassandra.operatorName | default "cassandra-operator" }}
      securityContext:
        runAsUser: 1000
      tolerations:
        - operator: Exists
      containers:
      - name:  {{ .Values.cassandra.operatorName | default "cassandra-operator" }}
        image: {{ .Values.global.image.repository }}/cassandra-operator:{{ .Values.cassandra.operatorVersion}}
        imagePullPolicy: {{ .Values.global.image.imagePullPolicy }}
        command:
        - casskop
        readinessProbe:
          exec:
            command:
            - /health
          initialDelaySeconds: 4
          periodSeconds: 10
          failureThreshold: 1
        resources:
          requests:
            cpu: 10m
            memory: 10Mi
          limits:
            cpu: 1
            memory: 512Mi
        env:
        - name: WATCH_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: OPERATOR_NAME
          value:  {{ .Values.cassandra.operatorName | default "cassandra-operator" | quote }}
        - name: LOG_LEVEL
          value: Debug
